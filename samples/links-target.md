# Evaluation harness (internal target)

This is the document an **internal `.md` link** opened **in place**. Notice the breadcrumb pill above — clicking it returns to the source doc *and its exact scroll position*.

## Detail

We sample 500 needle-in-haystack prompts per context length and measure exact-match recall. Each run is seeded so the degradation curve is reproducible.

Back to the start via the breadcrumb, or follow another [external reference](https://example.com/reference).
